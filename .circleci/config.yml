version: 2.1
jobs:
  build-and-test:
    docker:
      - image: maven:3.9-eclipse-temurin-17
    steps:
      - checkout
      - run:
          name: Download Dependencies
          command: mvn dependency:go-offline
      - run:
          name: Run Tests
          command: mvn verify
workflows:
  main-workflow:
    jobs:
      - build-and-test